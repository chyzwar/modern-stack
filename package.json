{
  "name": "dekra-claims",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --parallel",
    "start:prod": "lerna run start:prod --parallel",
    "build": "tsc --build",
    "build:watch": "tsc --build --watch"
  },
  "dependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.8.3"
  }
}
